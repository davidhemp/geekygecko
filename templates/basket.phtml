{% include 'header.phtml' %}

<div class="container">
    <div class="row">
        <div class="col-ld-12">
            {% for item in purchases %}
                <P> {{ item.id }}, {{ item.quantity }}, {{ item.price }}, {{ item.totalCost }}</p>
            {% endfor %}
        </div>
        <div class="col-ld-12">
            {{ subTotal }}
        </div>
    </div>
<div>
